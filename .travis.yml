language: python
sudo: required
python:
  - "3.6"
# command to install dependencies
install:
  - pip install pytest
  - pip install .
# command to run tests
script:
  - cd python_codon_tables
  - python -m pytest
